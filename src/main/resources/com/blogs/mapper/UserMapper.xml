<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blogs.mapper.UserMapper">

  <select id="getLoginUser" resultType="com.blogs.entity.User">
    SELECT *
    FROM user
    WHERE (`uname` = #{account} or `email` = #{account}  or `phone` = #{account})
      AND pwd = md5(#{pwd})
  </select>
</mapper>